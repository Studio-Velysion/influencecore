import { defaultSystem } from '@chakra-ui/react'

// Dans Chakra UI v3, on utilise defaultSystem directement
// Les couleurs personnalisées sont appliquées via CSS variables dans ClientChakraProvider
const theme = defaultSystem

export default theme
