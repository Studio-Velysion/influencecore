[variables]
# Postiz a besoin de Node 22+, et Coolify utilise Nixpacks.
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"


