# ⚡ Installation Rapide - Chakra UI v3

## 🎯 Solution Rapide

### Étape 1 : Installer pnpm (si nécessaire)

Dans PowerShell, exécutez :

```powershell
npm install -g pnpm
```

### Étape 2 : Installer les dépendances Chakra UI

```powershell
cd "H:\Studio Velysion CreatorHub\InfluenceCore\postiz-app-main"
pnpm add @chakra-ui/react@next @emotion/react@^11.13.0 @emotion/styled@^11.13.0 framer-motion@^11.0.0
```

### Alternative : Utiliser npm

Si pnpm ne fonctionne toujours pas :

```powershell
cd "H:\Studio Velysion CreatorHub\InfluenceCore\postiz-app-main"
npm install @chakra-ui/react@next @emotion/react@^11.13.0 @emotion/styled@^11.13.0 framer-motion@^11.0.0
```

## ✅ Vérification

Après l'installation, vérifiez :

```powershell
cd "H:\Studio Velysion CreatorHub\InfluenceCore\postiz-app-main"
pnpm list @chakra-ui/react
```

Ou avec npm :

```powershell
npm list @chakra-ui/react
```

## 🚀 Démarrer l'Application

Une fois les dépendances installées :

```powershell
cd "H:\Studio Velysion CreatorHub\InfluenceCore\postiz-app-main"
pnpm run dev
```

## 📋 Commandes Complètes (Copier-Coller)

### Option 1 : Avec pnpm

```powershell
# Installer pnpm globalement
npm install -g pnpm

# Aller dans le répertoire
cd "H:\Studio Velysion CreatorHub\InfluenceCore\postiz-app-main"

# Installer Chakra UI
pnpm add @chakra-ui/react@next @emotion/react@^11.13.0 @emotion/styled@^11.13.0 framer-motion@^11.0.0
```

### Option 2 : Avec npm

```powershell
# Aller dans le répertoire
cd "H:\Studio Velysion CreatorHub\InfluenceCore\postiz-app-main"

# Installer Chakra UI
npm install @chakra-ui/react@next @emotion/react@^11.13.0 @emotion/styled@^11.13.0 framer-motion@^11.0.0
```

## 🎉 C'est Tout !

Une fois installé, tous les composants Chakra UI seront fonctionnels !

